function App() {
  return (
    <main className="min-h-screen bg-canvas px-page py-section text-ink sm:px-8">
      <div className="mx-auto flex min-h-[calc(100vh-4rem)] w-full max-w-6xl items-center justify-center">
        <section className="w-full max-w-md rounded-card bg-surface p-8 text-center shadow-card ring-1 ring-border sm:p-10">
          <p className="text-caption font-semibold uppercase tracking-[0.2em] text-brand">
            Checkout de producto
          </p>
          <h1 className="mt-4 font-display text-title font-semibold tracking-tight">
            Base del frontend
          </h1>
          <p className="mt-4 text-body text-muted">
            La base de la aplicación está lista para la siguiente iteración de UI.
          </p>
        </section>
      </div>
    </main>
  )
}

export default App
